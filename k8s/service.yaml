apiVersion: v1
kind: Service
metadata:
  name: email-automation
  labels:
    app: email-automation
spec:
  selector:
    app: email-automation
  ports:
    - name: http
      port: 8085
      targetPort: 8085
  type: ClusterIP